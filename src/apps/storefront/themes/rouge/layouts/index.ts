/** Rouge layout registry — the `default` global chrome shell. */
import type { LayoutMap } from '../../../theme-engine/rendering';
import { DefaultLayout } from './DefaultLayout';
export const rougeLayouts: LayoutMap = { default: DefaultLayout };
